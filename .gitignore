# build artifacts
*.o
pca
enumerate
gen_tasks
gpu_enumerate
gpu_enumerate_oep
gpu_hybrid
gpu_hybrid_native
libpca.so
test_search_state*

# Training data (regenerable via gen_tasks + gen_dataset.sh)
data/train/
data/train_deep/
data/train_boot/
data/val/

# Run logs (kept on-box; contain absolute paths — do not commit to public repo)
logs/
data/*.bin

# Model checkpoints
checkpoints/

# TensorBoard logs
runs/
__pycache__/
*.pyc
